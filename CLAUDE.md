# dev-wiki

업무 중 겪은 **트러블슈팅 정리**와 **데일리 체크리스트**를 기록하는 Obsidian 보관함.

## 폴더 구조
- `데일리 체크리스트.md` — 루트의 단일 데일리 문서 (항상 오늘 섹션 하나만 유지)
- `knowledge/` — 지식 노트 (atomic, **플랫 — 하위 폴더를 만들지 않는다**, `#tag` + `[[링크]]`로 연결)
  - 한 노트가 여러 범주에 걸치므로 폴더로 나누지 않는다. 분류는 **영역 태그**로, 탐색은 **MOC 노트**로 해결한다.
- `cs/` — CS 처리 기록 (제품 구분은 `#qshop` / `#quv` 태그로)
- `assets/` — 노트에 첨부하는 이미지 등 첨부 파일 (루트에 두지 않는다)

### CS 노트 규칙
- "이 CS를 이렇게 처리했다"는 **처리 기록(현상·원인·처리)만 있어도 충분**하다.
- 기술적인 내용이 포함되면 `knowledge/`에 개념 노트로 분리하고 CS 노트에서 `[[링크]]`로 연결한다.

## 노트 저장 규칙
사용자가 대화 내용을 "노트에 저장해줘"라고 하면 **`save-notes` 스킬**을 따른다.
핵심: 한 덩어리가 아니라 **개념 단위로 쪼갠 atomic 노트 여러 개**를 `knowledge/`에 만들고, 태그 + `[[링크]]`로 연결한다 (제텔카스텐 방식).
- 노트 1개 = 개념 1개.
- frontmatter `tags`는 **영역 태그 1개를 맨 앞에 두고**, 그 뒤에 그 문서를 대표하는 **핵심 키워드**를 단다 (예: `browser-rendering`, `pixel-snapping`, `subpixel-rendering`).
  - 영역 태그는 MOC와 짝을 이루는 큰 분류다: `java` / `react` / `browser-rendering` / `dependency-management` / `nextjs` / `seo`. 해당하는 영역이 없으면 새로 만들지 말고 핵심 키워드만 단다.
  - `knowledge`, `settings`처럼 일반적·범용적인 분류 태그는 달지 않는다. 버그 해결 노트에는 `troubleshooting`을 추가할 수 있다.
- 새 노트끼리, 그리고 관련된 기존 노트와 `[[링크]]`로 연결한다 (고립 노트 금지).
- 기존 노트에 이미 있는 개념이면 새로 만들지 말고 보강하거나 링크만 추가한다.
- **`[[링크]]`는 직접적 개념 연관(인과·의존·정의 관계)이 있을 때만 건다.** "같은 범주에 속한다"는 식의 카테고리성 묶음은 링크가 아니라 **공통 `#태그`로 처리**한다. (예: "이 개념이 원인이 된 CS 사례"는 링크 O, "브라우저 렌더링 차이로 생기는 또 다른 이슈"처럼 주제만 비슷한 형제 노트는 링크 X·태그 O)

### MOC (목차 노트)
노트가 쌓여도 폴더를 만들지 않는 대신, 영역별 **MOC(Map of Content)** 노트가 입구 역할을 한다. 현재 `[[Java]]`, `[[React]]`, `[[브라우저 렌더링]]`, `[[의존성 관리]]`, `[[Next.js와 SEO]]` 5개가 있다.
- MOC는 `moc` 태그 + 영역 태그를 달고, 본문은 **소제목으로 묶은 `[[링크]]` 목록**이다. 각 링크 뒤에 한 줄 요약을 붙인다.
- 새 노트를 만들면 해당 MOC 목록에 추가하고, 노트의 `## 관련 노트`에도 MOC를 역링크한다.
- **한 노트가 여러 MOC에 속해도 된다** — 폴더 대신 MOC를 쓰는 이유가 이것이다.
- 한 영역의 노트가 5개를 넘으면 MOC 신설을 검토한다. 그 전까지는 태그만으로 충분하다.

## 데일리 체크리스트 규칙
사용자가 "데일리 체크리스트"(또는 "데일리 노트 생성")라고 하면 **`daily-note` 스킬**을 따른다. 날짜별 파일을 새로 만들지 않고, **루트의 `데일리 체크리스트.md` 한 문서**를 오늘 기준으로 갱신한다. 문서에는 **`## YYYY-MM-DD` 섹션이 항상 하나만** 있고, frontmatter·H1 없이 그 아래 `### 오늘 일정` / `### 오늘 할 일` / `### 팔로업` 세 하위 섹션으로 구성한다. (메모성 항목도 `팔로업`에 기록한다 — 별도 `메모` 섹션 없음)
- **미완료 이월**: 직전 날짜 섹션의 `오늘 할 일`·`팔로업`에서 항목을 아이콘 그대로 오늘 섹션의 같은 하위 섹션으로 복사한다. **이월 제외 대상은 `[x]`(완료)·`[-]`(취소) 뿐**이며, 그 외(`[/]`진행중·`[ ]`미완료·`[!]`중요·`[?]`확인필요 등)는 모두 이월한다. **`[/]`진행중 항목은 끝날 때까지 매일 이어서 남긴다.** `오늘 일정`은 이월하지 않고 매일 새로 채운다.
- **지난 섹션 삭제**: 이월을 마치면 직전 날짜 섹션은 통째로 지운다. 완료 항목도 함께 사라지므로 **그날 기록이 필요하면 삭제 전에 커밋**해 git 히스토리에 남긴다.
- **오늘 일정**: 본인(`rea1109@squares.ai`)이 참석자인 Google Calendar 이벤트만 시간순으로 넣는다.

## 스타일 컨벤션
노트를 쓸 때 아래 매핑대로 마크다운 스타일을 **목적에 맞게** 적용한다. 문법 예시는 `[[스타일 가이드]]` 참고.
**원칙: 과용 금지 — 다양하게 쓰는 게 목표가 아니라, 의미가 있을 때만 쓴다.**
- 핵심 결론·중요 단어 → **굵게** (남발 금지)
- 나중에 다시 찾을 핵심 용어 → `==하이라이트==`
- 주의/팁/정보/질문 → 콜아웃 `> [!warning]` `> [!tip]` `> [!info]` `> [!question]`
- 항목·옵션 비교 → 표
- 할 일·상태 → Minimal 체크박스 (`[ ]`할일 `[x]`완료 `[/]`진행중 `[!]`중요 `[?]`확인필요 `[-]`취소)
- 명령·파일명·코드 → `인라인 코드` / 코드블록(**언어 명시**)
- 흐름·관계·구조 → Mermaid 다이어그램
- 개념 연결 → `[[위키링크]]`, 분류 → `#태그`

## 커밋 컨벤션

회사 gitmoji 컨벤션을 따르며, 위키 특성상 아래 타입을 주로 사용한다.

**제목 형식:** `<이모지> <타입>: <요약>`

| 이모지 | 타입 | 사용 |
| --- | --- | --- |
| 📝 | Docs | 노트 내용 작성/수정 |
| ➕ | Add | 새 노트/파일 추가 |
| 🔥 | Remove | 노트/파일 삭제 |
| 🚚 | Move | 노트 이동/이름 변경 |
| ♻️ | Update | 템플릿/구조 개선 |
| 👷 | Chore | 설정(`.obsidian` 등) 변경 |

- 전체 타입 목록은 `/commit` 스킬 참조.
- 본문은 한국어로, **변경 이유**와 **변경 내용**을 서술한다.
- 마지막 줄에 항상 `Co-Authored-By: Claude <noreply@anthropic.com>`를 추가한다.
- 한 커밋에는 하나의 목적만 담는다.

**예시**
```
📝 Docs: Docker 컨테이너 DNS 해석 실패 트러블슈팅 정리

- 컨테이너 내부에서 외부 도메인 해석이 간헐적으로 실패하던 문제 기록
- 원인(systemd-resolved 설정)과 해결 방법, 재발 방지책 추가

Co-Authored-By: Claude <noreply@anthropic.com>
```

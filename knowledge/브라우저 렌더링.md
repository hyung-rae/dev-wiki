---
tags:
  - browser-rendering
  - moc
---
브라우저가 화면을 그리는 과정에서 생기는 문제들의 목차. 대부분 **CSS 값은 맞는데 화면이 다른** 상황이고, 실제 CS로 이어진 사례가 많다.

## 서브픽셀과 스케일링
- [[CSS 1px 테두리 렌더링 이슈]] — 소수점 좌표가 1px 테두리를 지운다
- [[CSS image-rendering 속성]] — 이미지 보간 알고리즘 지정

## GPU 합성
- [[GPU 합성 레이어와 빈 텍스처]] — 0px로 퇴화한 레이어가 재래스터화되지 않는 문제
- [[CSS 2D transform vs 3D transform 래스터화 차이]] — 3D 변형만 버그에 걸리는 이유

## 레이아웃
- [[CSS align-items flex-start vs baseline]] — 정렬 기준선의 차이

## 디버깅
- [[DevTools를 열면 사라지는 렌더링 버그]] — 증상만으로 원인 계층을 좁히기
